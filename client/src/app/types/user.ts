export interface UserForAuth {
    name: string;
    photo: string;
    city: string;
    email: string;
    password: string;
}

export interface ProfileDetails {
    username: string;
    photo: string;
    city: string;
    email: string;
}